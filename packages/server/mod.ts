/**
 * `@casys/mcp-server` — deprecated alias of the Casys MCP Platform framework.
 *
 * The framework moved to `@casys/mcp-platform` in 0.28.0. This package keeps
 * the old name working by re-exporting it: same API, no changes. New code
 * should import `@casys/mcp-platform` directly.
 *
 * @deprecated Use `@casys/mcp-platform` instead.
 */
export * from "@casys/mcp-platform";
